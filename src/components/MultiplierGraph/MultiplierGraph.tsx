import { Box } from "../Box";

const MultiplierGraph = () => {
  const value = Math.random() * 10;
  return (
    <Box h="500px" w="500px">
      MultiplierGraph
    </Box>
  );
};

export default MultiplierGraph;
